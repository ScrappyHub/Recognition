{
  "manifest_version": 3,
  "name": "Recognition Hello (Review)",
  "version": "1.0.0",
  "description": "Sample extension requesting a sensitive permission (tabs) so the policy flags it for review.",
  "permissions": ["storage", "tabs"],
  "action": { "default_title": "Hello Review" },
  "background": { "service_worker": "background.js" }
}
